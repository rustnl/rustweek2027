---
import HeroImage from "../../../public/svg/logo.svg?raw";

interface Props {
    logo?: boolean;
}

const { logo = false } = Astro.props;
---

<div id="week-schedule" class:list={["schedule container", logo]}>
    {
        logo && (
            <div class="image-wrapper">
                <Fragment set:html={HeroImage} />
            </div>
        )
    }
    <div class="days">
        <div class="mon">Monday May&nbsp;24</div>
        <div class="tue">Tuesday May&nbsp;25</div>
        <div class="wed">Wednesday May&nbsp;26</div>
        <div class="thu">Thursday May&nbsp;27</div>
        <div class="fri">Friday May&nbsp;28</div>
        <div class="sat">Saturday May&nbsp;29</div>
        <slot />
    </div>

    <div class="extra">
        <slot name="extra" />
    </div>
</div>

<style lang="scss">
    @use "../../styles/variables.scss";
    @use "../../styles/breakpoint.scss";

    .schedule {
        display: grid;
        padding: 2em 2em 2em 2em;

        gap: 2em;
        grid-template-columns: auto;
        grid-template-rows: auto auto;
        align-items: center;

        position: relative;
        background: variables.$color-yellow-400;
        z-index: 0;


        @include breakpoint.smaller_than(md) {
            padding: 1em 1em 4em 1em;
        }

        .extra {
            text-align: center;
            a {
                color: unset;
            }
        }

        .birthday {
            position: absolute;

            transform: skew(0deg, -8deg) rotate(-12deg) translateY(-3.3em)
                translateX(-5em);

            color: white;
            font-style: italic;
            font-size: variables.$font-size-sm;
            width: min(10em, 80vw);
            text-align: right;
            line-height: 1em;

            z-index: 2;
        }

        .days {
            display: grid;

            gap: 0.2em;

            justify-content: center;

            grid-template-columns: auto;
            grid-template-areas:
                "mon"
                "mon-s"
                "tue"
                "tue-s"
                "wed"
                "wed-s"
                "thu"
                "thu-s"
                "fri"
                "fri-s"
                "sat"
                "sat-s";

            div {
                position: relative;

                text-align: center;
                font-family: "Alfa Slab One", serif;
                font-size: variables.$font-size-md;

                margin-left: -1em;
                margin-right: -1em;

                @include breakpoint.smaller_than(lg) {
                    margin-top: 3rem;
                }

                @include breakpoint.wider_than(lg) {
                    font-size: calc(1.4 * variables.$font-size-sm);
                    margin-bottom: variables.space(3);
                }


                @include breakpoint.wider_than(xl) {
                    font-size: variables.$font-size-md !important;
                }
            }

            .mon {
                grid-area: mon;
            }
            .tue {
                grid-area: tue;
            }
            .wed {
                grid-area: wed;
            }
            .thu {
                grid-area: thu;

                @include breakpoint.smaller_than(lg) {
                    margin-top: 2em;
                }
            }
            .fri {
                grid-area: fri;
            }
            .sat {
                grid-area: sat;
            }

            @include breakpoint.wider_than(lg) {
                @include breakpoint.smaller_than(xl) {
                    grid-template-columns: repeat(3, 1fr);
                    grid-template-rows: auto;
                    grid-template-areas:
                        "mon   tue   wed"
                        "mon-s tue-s wed-s"
                        "thu   fri   sat"
                        "thu-s fri-s sat-s";

                    gap: 0em 1em;

                    justify-content: unset;

                    div {
                        font-size: variables.$font-size-lg;
                    }

                    .thu,
                    .fri,
                    .sat {
                        margin-top: 2em;
                    }
                }
            }

            @include breakpoint.wider_than(xl) {
                grid-template-columns: repeat(6, 1fr);
                grid-template-rows: auto;
                grid-template-areas:
                    "mon   tue   wed   thu   fri   sat"
                    "mon-s tue-s wed-s thu-s fri-s sat-s";

                gap: 0em 1em;

                justify-content: unset;

                div {
                    font-size: variables.$font-size-sm;
                }
            }
        }

        .image-wrapper {
            color: variables.$color-neutral-400;
            height: min(5em, 30vw);
            aspect-ratio: 3;
            align-self: center;
            justify-self: center;

            @include breakpoint.wider_than(md) {
                max-width: 60vw;
            }

            @include breakpoint.wider_than(xl) {
                max-width: 40vw;
            }
        }
    }
</style>
